// OBJECTS

// 1. Create a constant person object with name 'Mary' and age 25
const person = {
  name: "Mary",
  age: 25
}

// 2. Log the person's name
console.log(person.name);

// 3. Log the person's age
console.log(person.age);

// 4. Change the person's age to another number
person.age = 30;

// 5. Log the new age
console.log(person.age);