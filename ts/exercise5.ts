// ADDING WITH FUNCTION

// 1. Create an addition function that takes two parameters and adds them
// This function should return the addition
function addition(a: number,b: number){ //type needs to be specified
    const c= a+b;
    return c;
}
// 2. Execute the function with two example numbers and store the result in a variable
const result = addition(3,2);
// 3. Log the result
console.log(result);