//SUBTRACTING WITH FUNCTION

// 1. Create subtraction function that takes two parameters and adds them
// This function should return the subtraction
function subtract(x : number, y : number){
  return x - y;
}

// 2. Execute the function with two example numbers and store the result in a variable
const result = subtract(10, 2);

// 3. Log the result
console.log(result);
